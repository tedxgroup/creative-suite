import { RiImageLine } from "@remixicon/react"
import { PlaceholderView } from "@/components/placeholder-view"

export default function ImagensPage() {
  return (
    <PlaceholderView
      breadcrumb="/imagens"
      title="Imagens"
      icon={RiImageLine}
      description="Geração e edição de imagens — em breve."
    />
  )
}
